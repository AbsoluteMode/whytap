## What

## Why

## How it was tested

- [ ] `swift build`
- [ ] `swift test`
- [ ] Ran the app via `scripts/dev-run.sh --run` (if UI or hotkeys changed)

## Docs

`[docs: na]` or the paths you updated (`CLAUDE.md`, `docs/hotkey.md`,
`docs/build-and-release.md`, `docs/decisions/...`).
